void keyPressed() {
  /*
  if (keyCode == UP) {
    threshold++;
    if (threshold > 255) threshold = 255;
    //isDirty = true;
  } 
  
  if (keyCode == DOWN) {
    threshold--;
    if (threshold < 0) threshold = 0;
    //isDirty = true;
  } 
  
  if (key == ' ') {
    showOrig = !showOrig;
  }
  */
}